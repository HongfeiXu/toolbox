# 本地工具网页项目规划

## 项目概述
一个本地的工具网页，提供常用的功能转换和计算，包括色值转换、时间戳转换、单位转换等。所有功能都在客户端实现，无需后端支持。

## 技术栈选择
- **前端框架**：纯 HTML/CSS/JS
- **理由**：轻量级、无依赖、加载快速、易于维护
- **UI 风格**：简约风格

## 项目结构
```
project_0/
├── index.html          # 主页面
├── css/
│   └── style.css       # 样式（简约风格）
├── js/
│   ├── converters/     # 各种转换器的逻辑
│   │   ├── color.js    # 色值转换
│   │   ├── timestamp.js # 时间戳转换
│   │   └── units.js    # 单位转换
│   ├── converters-ext/ # 扩展工具（第二批）
│   │   ├── json.js     # JSON 格式化
│   │   ├── base64.js   # Base64 编码/解码
│   │   ├── uuid.js     # UUID 生成器
│   │   ├── wordcount.js # 字数统计
│   │   └── url.js      # URL 编码/解码
│   └── app.js          # 主应用逻辑（Tab 切换、UI 交互）
├── claude.md           # 项目规划文档
└── README.md           # 用户文档（完成后编写）
```

## 分批实现计划

### 第一批（核心功能）- 优先级最高
1. **色值转换** - HEX ↔ RGB ↔ HSL，支持颜色预览和复制
2. **时间戳转换** - 时间戳 ↔ 日期时间，支持毫秒/秒转换
3. **单位转换** - 存储单位（B/KB/MB/GB/TB）、长度、重量等

### 第二批（常用工具）- 优先级高
4. **JSON 格式化** - 验证、美化、压缩 JSON
5. **Base64 编码/解码** - 文本和 Base64 互转

### 第三批（开发者工具）- 可选
6. **UUID 生成器** - 快速生成 UUID v4
7. **字数统计** - 统计字符、单词、行数
8. **URL 编码/解码** - 处理特殊字符

## 进度记录

### 2025-11-08

#### 已完成
- [x] 创建项目结构
- [x] 编写 HTML 框架和 Tab 切换逻辑
- [x] 实现第一批功能（色值转换、时间戳转换、单位转换）
- [x] 实现第二批功能（JSON 格式化、Base64 编码/解码）
- [x] 添加样式（简约风格）
- [x] 测试第一批功能
- [x] 测试第二批功能

#### 待完成
- [ ] 编写 README.md
- [ ] 性能优化和最终测试

#### 已完成（所有三批功能）
- [x] 实现第三批功能（可选）
  - [x] UUID 生成器 - 快速生成 UUID v4
  - [x] 字数统计 - 统计字符、单词、行数
  - [x] URL 编码/解码 - 处理特殊字符

---

**最后更新**：2025-11-08 - 所有三批功能实现完成！

## 完整工具清单

| # | 工具名称 | 功能说明 | 批次 |
|---|--------|--------|------|
| 1 | 色值转换 | HEX ↔ RGB ↔ HSL，实时颜色预览 | 第一批 |
| 2 | 时间戳转换 | 秒/毫秒时间戳 ↔ 日期时间互转 | 第一批 |
| 3 | 单位转换 | 存储、长度、重量单位转换 | 第一批 |
| 4 | JSON 格式化 | 美化、压缩、验证 JSON | 第二批 |
| 5 | Base64 编码 | 文本 ↔ Base64 互转，UTF-8 支持 | 第二批 |
| 6 | UUID 生成器 | 快速生成 UUID v4，支持批量生成 | 第三批 |
| 7 | 字数统计 | 统计字符、单词、行数 | 第三批 |
| 8 | URL 编码 | URL 编码/解码，处理特殊字符 | 第三批 |
